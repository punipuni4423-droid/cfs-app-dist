# PDU 共有初期値

この共有パッケージで新規に起動したCFSは、PDU換算値を次の初期値で開始します。

| 項目 | 値 |
| --- | --- |
| VA / PDU | 3.3 |

設定は各PCのブラウザーに保存されます。すでにCFSを利用しているPCの設定値は、アプリ更新では上書きされません。既存導入先にも同じ値を適用する場合は、`Settings` の `PDU Settings` で `VA / PDU` を `3.3` に設定してください。

Device Master のPDU値・VA値は、このパッケージに含まれる標準デバイス定義を使用します。
